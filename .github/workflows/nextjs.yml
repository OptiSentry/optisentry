# ===============================
# FILE: package.json
# (Passe Name/Version gern an. Wichtig: "build" exportiert statisch.)
# ===============================
{
  "name": "optisentry-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "autoprefixer": "10.4.19",
    "eslint": "8.57.0",
    "eslint-config-next": "14.2.5",
    "postcss": "8.4.38",
    "tailwindcss": "3.4.7",
    "typescript": "5.4.5"
  }
}

# ===============================
# FILE: next.config.mjs
# (statischer Export, kompatibel zu GitHub Pages/CDN)
# ===============================
/** @type {import('next').NextConfig} */
export default {
  output: 'export',
  images: { unoptimized: true },
  trailingSlash: true,
  reactStrictMode: true,
};

# ===============================
# FILE: tsconfig.json
# ===============================
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "es2022"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "incremental": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "plugins": [{ "name": "next" }]
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}

# ===============================
# FILE: postcss.config.js
# ===============================
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

# ===============================
# FILE: tailwind.config.ts
# ===============================
import type { Config } from "tailwindcss";

export default {
  content: [
    "./app/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}"
  ],
  theme: {
    extend: {
      colors: {
        brand: {
          50:  "#f3f7ff",
          100: "#e6efff",
          200: "#c7dbff",
          300: "#9abaff",
          400: "#6a95ff",
          500: "#3f70ff",     // Primär
          600: "#2c56db",
          700: "#2344ad",
          800: "#1d3a8a",
          900: "#172f6b"
        }
      }
    }
  },
  plugins: [],
} satisfies Config;

# ===============================
# FILE: app/globals.css
# ===============================
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --bg: #0b1020;
  --fg: #e8ecf3;
}
html { scroll-behavior: smooth; }
body { background: var(--bg); color: var(--fg); }
a { @apply underline underline-offset-4 decoration-brand-500 hover:text-brand-400; }

.container {
  @apply mx-auto max-w-6xl px-4;
}

.btn {
  @apply inline-flex items-center justify-center rounded-xl px-5 py-3 font-medium transition;
}
.btn-primary {
  @apply bg-brand-500 hover:bg-brand-400 text-white shadow-lg shadow-brand-500/20;
}
.btn-ghost {
  @apply border border-white/15 hover:bg-white/5 rounded-xl px-5 py-3;
}

.card {
  @apply rounded-2xl border border-white/10 bg-white/5 p-6 backdrop-blur;
}

# ===============================
# FILE: app/layout.tsx
# (SEO/OG/Twitter + sauberes Grundlayout)
# ===============================
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  metadataBase: new URL("https://www.optisentry.com"),
  title: {
    default: "OptiSentry — Trust-First EvalOps & AI Audits",
    template: "%s · OptiSentry"
  },
  description:
    "OptiSentry liefert verifizierbare AI-Audits und EvalOps-Frameworks für Enterprise: Sicherheit, Compliance und Performance – messbar und nachvollziehbar.",
  alternates: { canonical: "https://www.optisentry.com/" },
  openGraph: {
    title: "OptiSentry — Trust-First EvalOps & AI Audits",
    description:
      "Verifizierbare AI-Audits. EvalOps-Frameworks. Nachweisbare Sicherheit & Performance.",
    url: "https://www.optisentry.com/",
    siteName: "OptiSentry",
    type: "website",
    images: [{ url: "/og.png", width: 1200, height: 630, alt: "OptiSentry" }]
  },
  twitter: {
    card: "summary_large_image",
    title: "OptiSentry — Trust-First EvalOps & AI Audits",
    description:
      "Verifizierbare AI-Audits. EvalOps-Frameworks. Nachweisbare Sicherheit & Performance.",
    images: ["/og.png"]
  },
  icons: [{ rel: "icon", url: "/favicon.svg" }]
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="de">
      <body>
        <header className="border-b border-white/10">
          <nav className="container flex h-16 items-center justify-between">
            <div className="flex items-center gap-2">
              <span className="h-3 w-3 rounded-full bg-brand-500" />
              <span className="text-lg font-semibold tracking-tight">OptiSentry</span>
            </div>
            <div className="hidden gap-6 md:flex">
              <a href="#benefits">Vorteile</a>
              <a href="#how">Wie es funktioniert</a>
              <a href="#contact">Kontakt</a>
            </div>
            <a className="btn btn-ghost" href="#contact">Get Audit</a>
          </nav>
        </header>
        <main>{children}</main>
        <footer className="mt-24 border-t border-white/10">
          <div className="container py-10 text-sm text-white/70">
            <div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
              <p>© {new Date().getFullYear()} OptiSentry. All rights reserved.</p>
              <div className="flex gap-6">
                <a href="/privacy">Datenschutz</a>
                <a href="/imprint">Impressum</a>
              </div>
            </div>
          </div>
        </footer>
      </body>
    </html>
  );
}

# ===============================
# FILE: app/page.tsx
# (Hero, Trust-Proofs, CTA. 100% statisch.)
# ===============================
import Script from "next/script";

export default function Page() {
  return (
    <>
      {/* JSON-LD (Organisation) */}
      <Script id="ld-org" type="application/ld+json"
        dangerouslySetInnerHTML={{
          __html: JSON.stringify({
            "@context": "https://schema.org",
            "@type": "Organization",
            "name": "OptiSentry",
            "url": "https://www.optisentry.com",
            "logo": "https://www.optisentry.com/favicon.svg",
            "sameAs": []
          })
        }}
      />

      {/* HERO */}
      <section className="container py-20 md:py-28">
        <div className="max-w-3xl">
          <h1 className="text-4xl md:text-6xl font-semibold leading-tight">
            Trust-First EvalOps<br/> und AI-Audits für Enterprise
          </h1>
          <p className="mt-5 text-lg text-white/80">
            Wir prüfen Risiken, Compliance und Performance Ihrer KI-Systeme —
            mit reproduzierbaren Benchmarks, Audit-Trail und klarem ROI.
          </p>
          <div className="mt-8 flex flex-wrap gap-4">
            <a className="btn btn-primary" href="#contact">Get Audit</a>
            <a className="btn btn-ghost" href="#how">See How</a>
          </div>
          <div className="mt-10 grid grid-cols-2 gap-4 text-white/60 md:grid-cols-4">
            <div className="card">SOC2-ready</div>
            <div className="card">ISO 42001-aligned</div>
            <div className="card">Model-Risk Controls</div>
            <div className="card">Audit-Trail</div>
          </div>
        </div>
      </section>

      {/* BENEFITS */}
      <section id="benefits" className="container py-16">
        <h2 className="text-2xl md:text-3xl font-semibold">Warum OptiSentry?</h2>
        <div className="mt-8 grid gap-6 md:grid-cols-3">
          <article className="card">
            <h3 className="text-xl font-medium">Risiken messbar senken</h3>
            <p className="mt-2 text-white/80">
              Guardrails, Red-Team-Findings und Incident-Szenarien werden quantifiziert und priorisiert.
            </p>
          </article>
          <article className="card">
            <h3 className="text-xl font-medium">Compliance nachweisen</h3>
            <p className="mt-2 text-white/80">
              Evidence-Sammlung (Logs, Datasets, Tests) für Audits und Regulatorik – jederzeit exportierbar.
            </p>
          </article>
          <article className="card">
            <h3 className="text-xl font-medium">Performance steigern</h3>
            <p className="mt-2 text-white/80">
              Eval-Suites & Benchmarks für Genauigkeit, Robustheit, Kosten und Latenz – CI-integriert.
            </p>
          </article>
        </div>
      </section>

      {/* HOW IT WORKS */}
      <section id="how" className="container py-16">
        <h2 className="text-2xl md:text-3xl font-semibold">So arbeiten wir</h2>
        <ol className="mt-6 grid gap-6 md:grid-cols-3">
          <li className="card">
            <h3 className="font-medium">1) Scope & Risks</h3>
            <p className="mt-2 text-white/80">
              System-Mapping, Datenflüsse, Threat-Modeling und KPI-Definition.
            </p>
          </li>
          <li className="card">
            <h3 className="font-medium">2) EvalOps</h3>
            <p className="mt-2 text-white/80">
              Test-Harness, Benchmarks, Red-Team-Prompts, Drift-Monitoring.
            </p>
          </li>
          <li className="card">
            <h3 className="font-medium">3) Report & Controls</h3>
            <p className="mt-2 text-white/80">
              Findings, Remediations, Control-Set & Management-Summary.
            </p>
          </li>
        </ol>
      </section>

      {/* CONTACT */}
      <section id="contact" className="container py-20">
        <div className="card">
          <h2 className="text-2xl md:text-3xl font-semibold">Lassen Sie uns starten</h2>
          <p className="mt-2 text-white/80">
            Schreiben Sie uns Ihr Zielsystem & Use-Case. Wir antworten innerhalb von 24 Stunden.
          </p>
          <div className="mt-6 flex flex-col gap-3 md:flex-row">
            <a className="btn btn-primary" href="mailto:info@optisentry.com?subject=Audit%20Request">
              info@optisentry.com
            </a>
            <a className="btn btn-ghost" href="https://www.linkedin.com" target="_blank">
              LinkedIn
            </a>
          </div>
        </div>
      </section>
    </>
  );
}

# ===============================
# FILE: app/privacy/page.tsx
# ===============================
export default function Privacy() {
  return (
    <div className="container py-16">
      <h1 className="text-3xl font-semibold">Datenschutzerklärung</h1>
      <p className="mt-6 text-white/80">
        Diese statische Seite dient als Platzhalter. Ersetze den Inhalt durch deine gültige Datenschutzinformation.
      </p>
    </div>
  );
}

# ===============================
# FILE: app/imprint/page.tsx
# ===============================
export default function Imprint() {
  return (
    <div className="container py-16">
      <h1 className="text-3xl font-semibold">Impressum</h1>
      <p className="mt-6 text-white/80">Platzhalter für Anbieterkennzeichnung gem. §5 TMG.</p>
    </div>
  );
}

# ===============================
# FILE: app/robots.ts
# (Next.js Metadata-Route; statische Ausgabe)
# ===============================
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: "*", allow: "/" },
    sitemap: "https://www.optisentry.com/sitemap.xml"
  };
}

# ===============================
# FILE: app/sitemap.ts
# (statische Sitemap; erweitert sich leicht um weitere URLs)
# ===============================
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  const base = "https://www.optisentry.com";
  const now = new Date().toISOString().split("T")[0];
  return [
    { url: `${base}/`, lastModified: now, changeFrequency: "monthly", priority: 1 },
    { url: `${base}/privacy`, lastModified: now, changeFrequency: "yearly", priority: 0.3 },
    { url: `${base}/imprint`, lastModified: now, changeFrequency: "yearly", priority: 0.3 }
  ];
}

# ===============================
# FILE: public/CNAME
# (EXAKT so, ohne Leerzeile am Anfang)
# ===============================
www.optisentry.com

# ===============================
# FILE: public/favicon.svg
# ===============================
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120">
  <defs>
    <linearGradient id="g" x1="0" x2="1" y1="0" y2="1">
      <stop offset="0" stop-color="#6a95ff"/><stop offset="1" stop-color="#3f70ff"/>
    </linearGradient>
  </defs>
  <rect width="120" height="120" rx="24" fill="#0b1020"/>
  <circle cx="28" cy="36" r="6" fill="url(#g)"/>
  <path d="M28 36 L60 60 L92 36 L92 84 L60 108 L28 84 Z" fill="none" stroke="url(#g)" stroke-width="8"/>
</svg>

# ===============================
# FILE: public/og.png
# (Platzhalter-Hinweis: füge hier ein 1200x630 PNG ein.)
# ===============================
# -- Bitte lokal ein echtes 1200x630-Bild unter public/og.png ablegen --

# ===============================
# FILE: .github/workflows/pages.yml
# (optimiert: Cache + statischer Export)
# ===============================
name: Deploy Next.js to GitHub Pages

on:
  push: { branches: [ "main" ] }
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Restore Next.js cache
        uses: actions/cache@v4
        with:
          path: .next/cache
          key: ${{ runner.os }}-next-${{ hashFiles('**/package-lock.json') }}-${{ hashFiles('**/*.[jt]s', '**/*.[jt]sx') }}
          restore-keys: |
            ${{ runner.os }}-next-${{ hashFiles('**/package-lock.json') }}-

      - name: Install dependencies
        run: npm ci

      - name: Build (static export)
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

# ===============================
# FILE: .gitignore
# ===============================
node_modules
.next
out
.DS_Store
*.log

# ===============================
# FILE: README.md
# (Kurz)
# ===============================
# OptiSentry — Static Next.js on GitHub Pages

- `npm i`
- `npm run dev` (lokal)
- `npm run build` erzeugt `./out` (statisch)
- Push auf `main` → Actions deployt nach GitHub Pages.
- Custom Domain liegt in `public/CNAME`.
